name: Supabase CI

on:
  push:
    branches: ["main"]
    paths:
      - "supabase/migrations/**"
      - "supabase/config.toml"
  pull_request:
    branches: ["main"]
    paths:
      - "supabase/migrations/**"
      - "supabase/config.toml"
  # 수동 실행 버튼 추가
  workflow_dispatch:

jobs:
  db_push:
    # push 이벤트이거나 수동 실행(workflow_dispatch)일 때 실행
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref "$SUPABASE_PROJECT_REF"

      - name: Apply migrations (db push)
        run: yes | supabase db push -p "$SUPABASE_DB_PASSWORD"

      - name: Show migration sync status
        run: supabase migration list -p "$SUPABASE_DB_PASSWORD"

  validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start local Supabase
        run: supabase start

      - name: Apply migrations locally
        run: supabase db reset

      - name: Stop local Supabase
        if: always()
        run: supabase stop
