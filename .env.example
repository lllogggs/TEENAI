# ======== Public (Client-safe) ========
# 브라우저에 노출되어도 되는 Supabase anon 키 세트
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ======== Server-only (NEVER expose to client) ========
# Gemini (서버에서만 사용)
GEMINI_API_KEY=

# Supabase Service Role Key (서버에서만 사용)
SUPABASE_SERVICE_ROLE_KEY=

# ======== Optional / Tuning ========
# 레이트리밋 튜닝(없으면 기본값 사용)
CHAT_RATE_LIMIT_WINDOW_SEC=60
CHAT_RATE_LIMIT_MAX=20
SUMMARY_RATE_LIMIT_WINDOW_SEC=60
SUMMARY_RATE_LIMIT_MAX=10
PROFILE_RATE_LIMIT_WINDOW_SEC=60
PROFILE_RATE_LIMIT_MAX=10

# 요약 트리거 튜닝
SUMMARY_IDLE_MIN_SEC=60
SUMMARY_EVERY_N_MESSAGES=6
SUMMARY_MAX_HISTORY_MESSAGES=30

# (선택) 프로덕션에서만 허용할 도메인/CORS 제어에 사용 가능
ALLOWED_ORIGINS=
